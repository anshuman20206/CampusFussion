
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for prototyping and development
    // This resolves permission-denied errors that can cause UI hanging
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
