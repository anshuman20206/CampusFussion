This directory is for static assets like images, fonts, etc.

Files in this directory are served from the root of the application. For example, a file at `/public/logo.png` can be accessed in your code as `/logo.png`.
